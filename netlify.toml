[build]
  command = "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/mockup-sandbox run build"
  publish = "artifacts/mockup-sandbox/dist"

[build.environment]
  NODE_VERSION = "20"
  PORT = "3000"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = "200"
